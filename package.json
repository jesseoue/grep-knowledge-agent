{
  "name": "grep-knowledge-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted knowledge agent on Railway — grep, find, and cat across your sources. No embeddings, no vector DB, no Vercel lock-in.",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*",
    "sandbox-service"
  ],
  "scripts": {
    "dev": "bun run --filter ./apps/web dev",
    "build": "bun run --filter ./apps/web build",
    "start": "bun run --filter ./apps/web start",
    "test": "bun test tests",
    "verify": "bun run test && bun run typecheck && bun run check:recovery && bun run build",
    "typecheck": "bun run --filter ./apps/web typecheck && bun run --filter ./packages/agent typecheck && bun run --filter ./packages/sdk typecheck && bun run --filter ./sandbox-service typecheck",
    "db:generate": "bun run --filter ./apps/web db:generate",
    "db:migrate": "bun run --filter ./apps/web db:migrate",
    "db:push": "bun run --filter ./apps/web db:push",
    "check:recovery": "node apps/web/scripts/recover-owner.mjs --help",
    "owner:recover": "node apps/web/scripts/recover-owner.mjs",
    "sandbox": "bun run sandbox-service/index.ts"
  },
  "packageManager": "bun@1.3.14",
  "devDependencies": {
    "typescript": "5.9.3",
    "vue-router": "5.2.0"
  }
}
